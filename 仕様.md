# ぷよぷよ（Elixir + ex_ratatui 版）仕様書

## 1. 概要
コンソールでプレイできる「ぷよぷよ」の1人用ゲーム。
参照実装（Tetris）と同じく「純粋関数のゲームロジック」と
「ex_ratatui による TUI/イベントループ」を分離し、ロジックは
ExUnit でテスト可能にする。

- Elixir 1.19 / OTP 28
- 依存: `ex_ratatui ~> 0.13.0`
- 実装構成: `Board` / `Game` / `Puyo` / `UI` / `Application`

## 2. 盤面
- 横幅 **2 列**（col 0..1）、高さ **12 行**（row 0..11）
- 表現: 行優先のフラットリスト（24 セル）。添字 `row * 2 + col`
- セル値: `nil`（空き）またはぷよ色 `:red | :green | :blue | :yellow`
- 表示は row 0 が上（天井）、row 11 が下（床）

## 3. 基本ルール（1 ターン）
1. 同じ色のぷよを **2 個縦に積んだペア**が盤面上段に出現する。
2. プレイヤーが以下を入力し、ペアを好きな列・深さに配置する：
   - `←`/`a` 左移動、`→`/`d` 右移動（1 列ずつ、壁・障害物に阻まれる）
   - `↓`/`s` ソフトドロップ（1 段下、+1 点/段）
   - `Space` ハードドロップ（最下部まで落下・固定、+2 点/段）
   - 重力：時間経過で 1 段ずつ自動落下
3. ペアが床 or 障害物に着地すると **固定**される。
4. 固定後に **消去判定 → 重力落下** を繰り返す（= チェーン）。
   - 同じ色で **4 つ以上直交連結**したぷよ群はまとめて消える。
   - 対角線連結は含めない（上下左右のみ）。
5. 連鎖は「消去 → 落下 → 再び消去」がなくなるまで続く。
6. 全連鎖が確定したら次のペアを出現させ、1 に戻る。

### ペアの表現
`%{color: c, row: r, col: cl}`
- 2 セルを占有: `(r, cl)` と `(r+1, cl)`
- 出現位置: row 0、col は 0/1 のうち空いている方（両方塞がれなければ col 0）
- 「はまるか」判定 `fits_pair?`: 両セルが盤面内かつ空きであること

## 4. 消去・連鎖・スコア
- 連結判定: BFS（連結成分）。成分サイズ ≥ 4 を消去対象とする。
- 連鎖（chain）: 1 回目 = chain 1、2 回目 = chain 2、…
- 1 連鎖あたりの得点:
  ```
  base = 消したぷよ数 * 10
  base = 消したぷよ数 >= 10 のとき base * 2   （10ぷよボーナス）
  得点 = base * chain
  ```
- ソフトドロップ +1/段、ハードドロップ +2/段 を別途加算。
- 表示用の集計: `score`（総得点）、`max_chain`（最長連鎖）、`total_cleared`（総消去数）。

## 5. ゲームオーバー
- 次のペアが出現位置（上段）に **はまらなければ** ゲームオーバー。
  （2 列のどちらもペア 2 セルが入らない場合）
- ゲームオーバー中は入力を無視（`q` 終了・`r` リスタート だけ有効）。

## 6. ランダム（次のペア）
- 4 色を 1 組（bag）として shuffle し、キューに積む。
- キューが 4 個以下になったら次の bag を補充。
- 表示には次の 3 ペアの色を提示する。

## 7. 操作一覧
| キー | 動作 |
| --- | --- |
| ← / a | 左移動 |
| → / d | 右移動 |
| ↓ / s | ソフトドロップ（+1/段） |
| Space | ハードドロップ（+2/段） |
| r | リスタート |
| q | 終了 |

## 8. 画面レイアウト（ex_ratatui）
- 左: 盤面（12 行 × 2 列）。ぷよは `●`（色つき）、ゴースト（着地予想）は `○`（dim）。
- 右: NEXT（次の 3 ペア）、ステータス（スコア/最長連鎖/総消去数）、操作ヘルプ。
- ゲームオーバー時は盤面上にオーバーレイ表示（R リスタート / Q 終了）。
- 全表示テキストは **日本語**。

## 9. 公開 API（テスト観点で重要）
- `Board.width/0, height/0, new/0, at/3, in_bounds?/2, set/4,
   fits_pair?/2, lock/2, settle/1, connected_components/1, clear_groups/1`
- `Game.new/0, spawn_next/1, move/2, soft_drop/1, hard_drop/1, tick/1,
   over?/1, chain_score/2, ghost_pair/1, next_pairs/1`
- `UI.display_grid/1, UI.apply_gravity/3`
- `Puyo.types/0, Puyo.color/1`

## 10. 非機能要件・制約
- ゲームロジック（Board/Game/Puyo）は外部副作用なしの純粋関数。
- TUI（UI）とロジックは分離し、ロジックをテストから直接検証できる。
- 描画は `ExRatatui.run/1` + `poll_event/1` のポーリング方式（参照実装準拠）。
- 重力タイミング判定 `apply_gravity/3` は純粋関数（絶対時刻ベース）で回帰テスト可能。
