# ぷよぷよ テスト仕様書

ロジック（Board / Game / Puyo）と UI の公開関数を ExUnit で検証する。
純粋関数中心のため、ランダムは seed を固定 or 直接状態を構築して再現可能にする。

## 1. Puyo
- `types/0` が 4 色 `[:red,:green,:blue,:yellow]` を返す。
- `color/1` が各タイプに対して表示色（原子 or {:rgb,...}）を返し、
  未知タイプは `:white` 等のデフォルトを返す（エラーにならない）。

## 2. Board
### 2-1 基礎
- `new/0` は長さ 24 の全 nil リスト。
- `at/3`: 範囲内は nil を返し、範囲外は `:out`。
- `in_bounds?/2`: 0..11 行 / 0..1 列のみ true。
- `set/4`: 指定セルに色を書き、それ以外は不変。

### 2-2 ペア
- `fits_pair?/2`: 両セルが空きなら true、1 つでも塞がれれば false、
  盤面外（row 11 で row+1 が越境）なら false。

### 2-3 連結・消去
- `connected_components/1`:
  - 縦に 4 つ同じ色 → 1 つの成分（サイズ 4）。
  - 2x2 の 4 つ → 1 つの成分。
  - 対角 4 つ → 互いに孤立した 4 つの成分（対角は連結しない）。
- `clear_groups/1`:
  - 4 つ連結 → その 4 セルが nil になり、返り値の消去数 = 4。
  - 3 つ連結 → 消去されない（消去数 0、盤面不変）。
  - 2 つの別々の 4 連結が同時に消去される。

### 2-4 重力
- `settle/1`:
  - 空中に浮いたぷよが床まで落ちる。
  - 列ごとに独立（片方の列のぷよが他列に横流れしない）。
  - 既に詰まった列は不変。

## 3. Game
### 3-1 初期化
- `new/0`: ペアが出現、score=0、over? は false、キュー ≥ 4。
- 出現ペアの 2 セルは空き（fits_pair? が true）。

### 3-2 移動
- `move/2`: 左右 1 列ずつ動ける。壁（col<0 / col>1）では動かない。
- 障害物が挟まったら動けない。

### 3-3 ドロップ
- `soft_drop/1`: 1 段下がり +1 点。底で止まると固定せずそのまま。
- `hard_drop/1`: 最下部まで落下し固定、連鎖処理が入り次のペアが出現。
- `tick/1`: 1 段下がり。着地したら固定＋連鎖処理。

### 3-4 連鎖・スコア
- 縦 4 つを作れる配置を固定 → 1 連鎖で 4 消去、score = 4*10*1 = 40。
- 落下後にさらに 4 つ連結して 2 連鎖になる配置 → score = 4*10*1 + 4*10*2。
- 10 個以上消した連鎖 → 10*10*2(chain1)*1 = 200（10ぷよボーナス）。
- `chain_score/2` の直接検証:
  - `chain_score(4,1)=40`、`chain_score(4,2)=80`、`chain_score(10,1)=200`、
    `chain_score(12,2)=480`。

### 3-5 ゲームオーバー
- 上段 2 行を塞いだ状態 → 次のペアが出現できず `over?` が true、
  message = "ゲームオーバー"。
- ゲームオーバー後は move/soft_drop/hard_drop/tick で状態が不変。

### 3-6 補助
- `ghost_pair/1`: 現在のペアの着地位置（最下部）を返す。
- `next_pairs/1`: キュー先頭 3 件を返す。
- 空盤面で `hard_drop` するとペアは最下段 (row 10, row 11) に固定される。

## 4. UI
- `display_grid/1`:
  - 12 行 × 2 列のグリッドを返し、各セルは `{color|nil, kind}`。
  - アクティブペア 2 セルが `:active`、ゴースト 2 セルが `:ghost`。
  - 静止（着地）位置ではゴーストと重複表示しない（active のみ）。
  - 固定セルは上書きされない。
- `apply_gravity/3`:
  - `now-last < gravity_ms` なら何も起きない（last_tick も不変）。
  - `>= gravity_ms` なら 1 ティック進み last_tick=now。
  - 3 倍経過しても 1 フレームで 1 ティックのみ。
  - 負の絶対時刻でも正しく判定される（回帰）。
  - ゲームオーバー中はティックしない。

## 5. doctest
- Board / Game の主要関数に doctest を付け、`doctest` をテストに組み込む。
