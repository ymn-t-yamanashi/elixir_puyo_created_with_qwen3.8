# ぷよぷよ ルール（本ゲーム版）

ターミナルで動く 1 人用ぷよぷよ風ゲームの**実際の実装ルール**です。
コード（`lib/game.ex` / `lib/board.ex` / `lib/ui.ex`）とテスト（61 件パス）で検証済み。
技術的な仕様は `仕様.md`、テスト内容は `テスト仕様.md` を参照。

## 1. 概要

- 相手（AI）なしの**スコアアタック型**
- 目標: 盤面が詰まる前に、4 つ連結で消して連鎖を重ね、高得点を狙う

## 2. 盤面

- **2 列 × 12 行**（全 24 セル）
- 行 0 が上（天井）、行 11 が下（床）
- 列が 2 つしかないため、ペア 1 つが 1 列全体を占有する

## 3. ぷよとペア

- 色は **4 色**: 赤・緑・青・黄
- **同色のぷよ 2 個を縦に積んだペア**が出現
- 上段（行 0）の空いている列に出現（ペアの 2 セルが空きなら出現可能）

## 4. 1 ターンの進行

1. 上段にペアが出現する
2. キーで移動・落下させる（重力: **800ms ごとに 1 段自動落下**）
3. 床 or 積み上がったぷよに着地すると**固定**される
4. **消去判定 → 落下 → 消去判定…** を繰り返す（= 連鎖）
5. 連鎖が確定すると次のペアが出現し、1 に戻る

## 5. 操作

| キー | 動作 | 備考 |
| --- | --- | --- |
| `←` / `a` | 左移動 | 1 列ずつ、壁・障害物に阻まれる |
| `→` / `d` | 右移動 | 1 列ずつ |
| `↓` / `s` | ソフトドロップ | 1 段下げる。**+1 点/段** |
| `Space` | ハードドロップ | 最下部まで落下・固定。**+2 点/段** |
| `r` | リスタート | ゲームオーバー時でも有効 |
| `q` | 終了 | いつでも有効 |

## 6. 消去のルール

- **同じ色のぷよが 4 つ以上、上下左右（直交）で連結**していれば、その連結グループは**まとめて消える**
- 対角線方向の連結は含まない
- 盤が 2 列しかないため、実際に起こりやすいのは「1 列に 4 個以上縦に積む」形

## 7. 連鎖（チェーン）

- 消去後、その上のぷよは**列ごとに独立して**最下部へ落下する
- 落下で新たに「4 つ以上の同色連結」ができたら、また消える
- これを「消去 → 落下 → 消去…」の繰り返し。対象が 0 になるまで続く
- 繰り返した回数が連鎖数（1 回目 = 連鎖 1、2 回目 = 連鎖 2、…）
- 最長連鎖 `max_chain` は記録される

## 8. スコア

### ドロップ点

| 操作 | 加点 |
| --- | --- |
| ソフトドロップ | +1 点 / 段 |
| ハードドロップ | +2 点 / 段 |

### 連鎖ごとの得点

```
base = 消したぷよ数 × 10
base = 消したぷよ数 >= 10 のとき base × 2   （10 ぷよボーナス）
その連鎖の得点 = base × 連鎖数
```

### 計算例

| 消した数 | 連鎖数 | 得点 |
| --- | --- | --- |
| 4 | 1 | 4×10 = **40** |
| 4 | 2 | 4×10×2 = **80** |
| 10 | 1 | 10×10×2 = **200** |
| 12 | 2 | 12×10×2×2 = **480** |

### ステータス（画面右に表示）

- `score`（総得点）
- `max_chain`（最長連鎖）
- `total_cleared`（総消去数）

## 9. 次のペア（乱数）

- **4 色を 1 組（bag）**としてシャッフルし、出現キューに積む
- キューが 4 個以下になると次の bag を補充（= 4 色が均等に必ず出る）
- 画面上の NEXT パネルに**次の 3 ペア**の色を表示

## 10. 画面表示

- `●` 固定済み・落下中のぷよ（各色のForeground）
- `○` **ゴースト**（着地予想位置、同色の dim 表示）
- 左: 盤面（12 行 × 2 列）
- 右: NEXT / ステータス / 操作ヘルプ
- ゲームオーバー時は盤面上にオーバーレイ表示

## 11. ゲームオーバー

- 上段の**どちらの列にもペアが出現できない**（2 セル分の空きがない）場合
- ゲームオーバー中は、`r`（リスタート）と `q`（終了）だけが有効

## 12. 実物のぷよぷよとの違い

| 項目 | 実物（近年作） | 本ゲーム |
| --- | --- | --- |
| 盤面 | 6 列 × 12 行 | **2 列 × 12 行** |
| 4 つ消し | 1000 点 | 40 点 |
| 5 つ消し | 5000 点 | 50 点 |
| 6 つ以上 | 15000 点 | 60〜90 点、10 個以上で ×2 |
| 連鎖倍率 | 基本点 × 連鎖数 | 同（× 連鎖数） |
| ドロップ点 | ソフト +1 / ハード +2 | 同 |
| 消去条件 | 同色 4 連結（直交） | 同 |
| ゲームオーバー | 上段以上で固定 | ペア出現不能 |
| 相手 | AI / 対戦 | **1 人（相手なし）** |
| 次の表示 | 3 個 | 3 個 |
| 乱数 | 7 バッグ等 | **4 色 1 組の bag** |
