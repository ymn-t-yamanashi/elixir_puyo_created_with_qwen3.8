# ぷよぷよex 実装調査結果（ルール.md と実装の突合）

- 調査日: 2026-08-18
- 対象: `ルール.md` の記載事項 vs コード実装
  - 実装: `lib/board.ex` / `lib/game.ex` / `lib/puyo.ex` / `lib/ui.ex` / `lib/puyopuyo.ex` / `lib/application.ex`
  - テスト: `test/*_test.exs`（5 ファイル）
- 方法: 各ルールの項目を実装コード・テストと 1 項目ずつ照合し、さらに `mix test` を実行して検証

## 総合結論

**ルール.md の全項目（12 セクション）が実装と一致している。** 不整合・矛盾は見つからなかった。
テスト 61 件（テスト 54 + ドキュメントテスト 7）すべてがパスすることも確認済み（後述）。

## 1. テスト実行結果

```
$ mix test
Running ExUnit with seed: 641025, max_cases: 64
.............................................................
Finished in 0.07 seconds (0.07s async, 0.00s sync)
7 doctests, 54 tests, 0 failures
```

- 合計 **61 件（54 tests + 7 doctests）、失敗 0** → ルール.md 冒頭の「テスト（61 件パス）で検証済み」の記載は**正確**。
- 内訳: `board_test.exs` 16 / `game_test.exs` 20 / `ui_test.exs` 13 / `puyo_test.exs` 4 / `puyopuyo_test.exs` 1（+ 各モジュールの doctest）

## 2. 項目別照合表

| # | ルール.md の記載 | 実装箇所 | 結果 |
| --- | --- | --- | --- |
| 2 | 盤面 2 列 × 12 行（24 セル）、行 0 = 上 | `Board.ex` `@width 2` / `@height 12` / `@size 24` | ✅ 一致 |
| 3 | 4 色（赤・緑・青・黄） | `Puyo.types()` = `[:red, :green, :blue, :yellow]` | ✅ 一致 |
| 3 | 同色 2 個を縦に積んだペア | `Board.pair_cells/1` = `[{row,col},{row+1,col}]`、`lock/2` は両セル同じ色 | ✅ 一致 |
| 3 | 上段（行 0）の空いている列に出現 | `Game.spawn_next/1` は row 0、col 0 → col 1 の順に `fits_pair?/2` で判定 | ✅ 一致（※注 1） |
| 4 | 重力 800ms で 1 段自動落下 | `Game.gravity_ms()` = 800、`UI.apply_gravity/3` が間隔毎に `Game.tick/1` | ✅ 一致 |
| 4 | 着地で固定 | `Game.tick/1` が下段に収まらなければ `lock_and_cascade/1` | ✅ 一致 |
| 4 | 消去 → 落下 → 消去…（連鎖） | `Game.cascade/2` が `clear_groups` + `settle` を 0 になるまで繰り返し | ✅ 一致 |
| 4 | 連鎖確定後、次のペア出現 | `lock_and_cascade/1` の末尾で `spawn_next/1` | ✅ 一致 |
| 5 | `←`/`a` 左・`→`/`d` 右（1 列、壁・障害物で阻まれる） | `UI.apply_key/2` → `Game.move/2`（範囲外・`fits_pair?` 失敗で無変化） | ✅ 一致（テストあり） |
| 5 | `↓`/`s` ソフトドロップ +1 点/段 | `Game.soft_drop/1`（下がる 1 段につき +1、着地でも加点のみ固定しない） | ✅ 一致 |
| 5 | `Space` ハードドロップ +2 点/段 | `Game.hard_drop/1`（落下距離 ×2 点、即時固定・連鎖処理） | ✅ 一致 |
| 5 | `r` リスタート（ゲームオーバー時でも有効） | `UI.apply_key/2` は `q` / `r` を**先に**扱い、`over?` ガードより優先 | ✅ 一致 |
| 5 | `q` 終了（いつでも有効） | `apply_key(_state, "q")` が最優先で `:quit` | ✅ 一致 |
| 6 | 同色 4 個以上・上下左右直交連結でまとめて消去 | `Board.clear_groups/1`（BFS 4 方向、成分 ≥ 4 で消去） | ✅ 一致（テストあり） |
| 6 | 対角線連結は含まない | BFS 隣接は `{-1,0},{1,0},{0,-1},{0,1}` のみ。対角 4 個 → 孤立 4 成分のテストで確認 | ✅ 一致 |
| 7 | 消去後、上のぷよは**列ごとに独立して**落下 | `Board.settle/1` が列ごとに下端詰め（横流れしないテストあり） | ✅ 一致 |
| 7 | 「消去 → 落下 → 消去…」が対象 0 まで | `cascade/2` の再帰ループ | ✅ 一致 |
| 7 | 繰り返した回数が連鎖数（1 回目 = 連鎖 1） | `Enum.with_index(1)` で連鎖番号を付与 | ✅ 一致 |
| 7 | 最長連鎖 `max_chain` を記録 | `lock_and_cascade/1` が `max(max_chain, 消去回数)` を更新 | ✅ 一致 |
| 8 | ソフト +1/段・ハード +2/段 | `soft_drop/1` / `hard_drop/1` | ✅ 一致 |
| 8 | `base = 消した数 × 10`、10 以上なら ×2、さらに ×連鎖数 | `Game.chain_score/2` | ✅ 一致 |
| 8 | 計算例 4/1→40、4/2→80、10/1→200、12/2→480 | `chain_score/2` の doctest が 4 例すべてを検証 | ✅ 一致 |
| 8 | ステータス: score / max_chain / total_cleared | `UI.stats_paragraph/1` に 3 項目表示 | ✅ 一致 |
| 9 | 4 色 1 組の bag をシャッフルしてキューに積む | `Game.build_bag/0` = `Puyo.types() |> Enum.shuffle()` | ✅ 一致 |
| 9 | キューが 4 個以下で次の bag を補充 | `Game.top_up/1`（`length(queue) <= 4` で 4 色追加） | ✅ 一致 |
| 9 | NEXT パネルに次の 3 ペアを表示 | `Game.next_pairs/1`（先頭 3 件）、`UI.next_paragraph/1` | ✅ 一致（※注 2） |
| 10 | `●` = 固定・落下中のぷよ（各色 fg） | `UI` `@cell "●"`、`Puyo.color/1` で前景色 | ✅ 一致 |
| 10 | `○` = ゴースト（着地予想、同色 dim） | `@ghost "○"`、`modifiers: [:dim]`、`ghost_pair/1` で着地位置算出 | ✅ 一致 |
| 10 | 左: 盤面 / 右: NEXT・ステータス・ヘルプ | `draw_all/1`（盤面 x:0〜9、右パネル x:12〜27） | ✅ 一致 |
| 10 | ゲームオーバーは盤面上にオーバーレイ | `game_over_paragraph/0` を盤面矩形内に x:1, y:6 で重ね描画 | ✅ 一致 |
| 11 | 上段の両列ともペアが出せない（2 セル空きなし）で終了 | `spawn_next/1` が col 0 / col 1 両方で `fits_pair?` 失敗時に `over: true` | ✅ 一致 |
| 11 | ゲームオーバー中は `r` / `q` のみ有効 | `move/soft_drop/hard_drop/tick` がいずれも `over?` で無変化（テストあり）。UI 側も `r` / `q` 以外のキーを無視 | ✅ 一致 |
| 12 | 実物との違い表（盤面 2×12、4 つ消し 40 点、5 つ 50 点、6〜9 つ 60〜90 点、10 以上 ×2、連鎖倍率 ×連鎖数、1 人用、NEXT 3 個、4 色 bag） | 上記各項の実装 | ✅ 一致 |

## 3. 補足・留意点（不整合ではないが確認した詳細挙動）

1. **出現列の優先順**（§3）: 2 列とも空なら**左列（col 0）が優先**され、左が塞がれている場合のみ右列に出る。ルール本文（「空いている列に出現」）と矛盾しない。
2. **ソフトドロップでの固定タイミング**（§4-3）: ソフトドロップで着地してもその場で固定されず、次の重力ティック（800ms 以内）で固定される。ハードドロップは即時固定。ルールの「着地すると固定」に整合する。
3. **重力のキャッチアップなし**（§4-2）: 1 フレームで 1 ティックのみ（`ui_test.exs`「3 倍経過しても 1 フレームで 1 ティックのみ」）。つまり 800ms ごとに厳密に 1 段。
4. **NEXT は常に 3 個表示される**（§9）: `top_up/1` の結果キューは常に 4 以上保持されるため、`take(3)` が常に 3 件返る。
5. **ゴーストの重複排除**（§10）: ペアが着地位置にいる場合、ゴーストはアクティブと重複せずアクティブのみ描画される（`display_grid/1` + テスト）。ルールの「着地予想位置」表示と矛盾しない。
6. **ゲームオーバー中の操作**（§11）: `q` / `r` は `over?` チェックより前に処理されるため、確かに「r と q のみ有効」。
7. **ファイル参照の有効性**: ルール.md が参照する `lib/game.ex` / `lib/board.ex` / `lib/ui.ex` / `仕様.md` / `テスト仕様.md` はすべて存在する。なお実装にはそれに加えて `lib/puyo.ex` / `lib/puyopuyo.ex` / `lib/application.ex` もある（参照漏れではない）。

## 4. 所見（コード・運用）

- ルール.md の記載は実装と忠実であり、**ドキュメントの正確性は高い**（テスト数「61 件」まで正確）。
- テストは `仕様.md` / `テスト仕様.md` の各項目に対応して構成されており、連鎖スコア計算例・対角非連結・列ごとの落下・ゲームオーバー時の無変化など、ルール本文の要点が網羅的にカバーされている。
- 軽微な運用メモ（ルールとは無関係、いずれも 8/18 午後に対処済み）:
  - `README.md` が mix 生成時のテンプレート（「TODO: Add description」）のままだった → プロジェクト固有の内容に書き換え済み（コミット `e054051`）。
  - リポジトリ直下に `erl_crash.dump`（5.4 MB）が混入していた（8/18 13:36 生成）→ 削除済み。`.gitignore` には `erl_crash.dump` が登録済み（git 管理外）。
  - 起動命令: 当初 `mix run --no-halt -e "Puyopuyo.play()"` を推奨していたが、`Puyopuyo.play/0` は終了時に `System.stop(0)` で VM を正常終了させるため `--no-halt` は不要。現在は `mix puyopuyo` タスクが推奨（`lib/mix/tasks/puyopuyo.ex`）。

## 5. 調査後のフォローアップ修正（2026-08-18 午後）

本調査は「ルール.md と実装の突合」を目的としており、以下は調査後に発見・修正した事項の記録である（ルール不整合ではない）。

| 事項 | 内容 | 状態 |
| --- | --- | --- |
| ぷよが正方形に見えた | `UI` の `@cell` が 2 文字（`"●●"`）だったため、縦積みペアが 2×2 の塊として描画されていた。`@cell` / `@ghost` / `@empty` を 1 文字（`"●"` / `"○"` / `" "`）に修正（コミット `00b9307`） | ✅ 修正・検証済み |
| 検証 | ① `mix test` 61 件全数パス ② ヘッドレスで `display_grid/1` から描画文字列を生成し「各行 2 文字・ペアは同一列に縦積み」をプログラム的に確認 ③ TTY キャプチャ（強制サイズ 24×80）で実描画の各行が `│●│`（1 行 1 ギリフ）であることを確認 | ✅ |
| README 書き換え | mix テンプレート → プロジェクト固有の内容（コミット `e054051`） | ✅ |
| 不要ファイル削除 | `erl_crash.dump`（5.4 MB）を削除 | ✅ |
| 起動タスク追加 | `mix puyopuyo`（`lib/mix/tasks/puyopuyo.ex`）を追加し、README・テスト（55 tests / 7 doctests）に反映 | ✅ |

## 6. 結論

ルール.md に沿って**正しく作成されている**。12 セクションすべてがコード実装・テストと一致し、61 件のテストが全数パスすることを確認した。
